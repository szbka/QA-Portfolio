# Database Testing

This project demonstrates basic SQL and database testing skills.

## Project Structure

- `data/` — test data
- `dml/` — SQL queries for working with data
- `schema/` — database structure, tables and relationships

## Tools

- PostgreSQL
- SQL
- DBeaver / pgAdmin
- GitHub
